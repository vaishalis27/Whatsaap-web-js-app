# SECURITY: Copy to .env and fill in. NEVER commit .env to git.
# Server
PORT=4000
NODE_ENV=production

# Security: API key required ONLY for external API calls (curl, Postman, other apps)
# Dashboard (browser) works without it. Generate with: openssl rand -hex 32
API_KEY=your-secure-random-api-key

# Restrict CORS to your domain(s). Comma-separated. Use * for development only.
ALLOWED_ORIGINS=https://yourdomain.com

# Optional: On VPS (Hostinger, etc.) use system Chromium if Puppeteer bundle fails
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium

# Anti-detection (optional - defaults are safe)
# MIN_MESSAGE_DELAY=2000
# MAX_MESSAGE_DELAY=5000
# COOLDOWN_PERIOD=30000
# MAX_MESSAGES_BEFORE_COOLDOWN=5
# MEDIA_DELAY_MULTIPLIER=1.5
